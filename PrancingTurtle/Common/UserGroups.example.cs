namespace Common
{
    // Uncomment this class, and create a name for the admin group
    // This can be anything you like, as long as it matches a record in the 'UserGroup' table
    // This will be used to check whether users should be given admin privileges or not.
    // When a user logs in, their group membership is checked (UserGroupMembership table) and any matching roles are added as Claims.

    //public static class UserGroups
    //{
    //    public const string Admin = "AdminUsers";
    //}
}